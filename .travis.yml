sudo: required
language: generic
services:
    - docker

before_install:
    - docker build pkdocker/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true pkdocker/docker-react npm run test -- --coverage